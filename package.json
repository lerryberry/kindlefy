{
  "name": "krystallise-fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "pm2:dev": "pm2 start ecosystem.config.js --only krystallise-backend-dev,krystallise-frontend-dev",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}